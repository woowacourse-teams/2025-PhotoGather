version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - cd frontend
      - npm ci

  build:
    commands:
      - npm run build:prod

artifacts:
  base-directory: frontend/dist
  files:
    - '**/*'
